<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.studentreportcard">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".StudentLogin"
            android:label="@string/title_activity_student_login" />
        <activity
            android:name=".TeacherLogin"
            android:label="@string/title_activity_teacher_login" />
        <activity
            android:name=".StudentproA"
            android:label="@string/title_activity_studentpro" />
        <activity android:name=".Marks" />
        <activity android:name=".TeacherProA" />
        <activity android:name=".StudentMarks" />
        <activity android:name=".TakeMarks"></activity>
    </application>

</manifest>